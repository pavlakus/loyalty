# QA Prompt: LP-000006

Act only as the independent QA Agent after Review approval. Read the LP-000006 specification, MIP, accepted ADR-009, implementation and review evidence, committed diff and current lifecycle records.

Validate every acceptance criterion and mandatory test: defaults, missing and invalid values, production behavior, safe errors, server-only separation, no public/secret variables, secret scan, build, lint, typecheck, focused tests, regression and rollback evidence. Do not modify runtime code or tests. Persist `implementation/evidence/LP-000006/qa.md` and approve only when no P0/P1 findings remain.

Return the full QA response contract.
