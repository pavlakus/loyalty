# LP-000008 Implementation Prompt

Implement only the READY LP-000008 Event Contract Foundation task. Read `AGENTS.md`, the task, MIP-000, the event catalog, ADR-004 and lifecycle evidence before changing approved files. Use a dedicated implementation branch, preserve unrelated worktree changes, implement only the generic versioned event envelope and runtime validation, run mandatory validation, persist `implementation/evidence/LP-000008/implementation.md`, and route to independent review only after a clean commit and synchronized status records.
