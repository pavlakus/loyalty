# LP-000009 Implementation Prompt

Implement only READY LP-000009. Read `AGENTS.md`, the task, MIP-000, ADR-003, ADR-007, ADR-009, ADR-010 and preparation evidence before changing files. Use a dedicated Database Agent branch. Implement only the ADR-010-approved `node-pg-migrate` migration boundary, PostgreSQL SQL-first immutable migrations, `DATABASE_URL` validation/redaction, database scripts and documentation within the allowed scope. Do not add business tables, outbox/idempotency/tenant/authentication behavior, pool environment variables or application-startup migrations. Run clean, upgrade, immutability, redaction and required repository validation. Persist `implementation/evidence/LP-000009/implementation.md` and route to independent review only after a clean isolated commit and synchronized status records.
