# Implementation Prompt: LP-000006

Act only as the Backend Developer Agent after LP-000006 is READY. Read the task, MIP, ADR-007, accepted ADR-009, lifecycle records and preparation evidence. Implement only typed startup validation for `NODE_ENV`, `PORT` and `HOST` within the approved files. Preserve defaults and production behavior; expose no client/public or secret configuration; add no future credentials or Loyalty behavior. Use an isolated backend branch, focused tests and exact repository validation. Persist implementation evidence and hand off only after an isolated commit and passing checks.

Return the full implementation response contract.
