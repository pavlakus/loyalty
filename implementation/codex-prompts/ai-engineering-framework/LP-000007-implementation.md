# Implementation Prompt: LP-000007

Act only as the Backend Developer Agent after LP-000007 is READY. Implement only the approved API contract package and API shared error mapping. Match MIP sections 11/12 and `docs/blueprint/43-api-contract.md`; define all typed categories; centralize HTTP mapping; never expose raw causes or infrastructure messages. Add contract and safe-mapping tests, use an isolated `agent/backend/LP-000007-api-contracts` branch, run exact validation, and persist implementation evidence.

Return the full response contract.
