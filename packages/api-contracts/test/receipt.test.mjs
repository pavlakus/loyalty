import assert from "node:assert/strict";
import test from "node:test";
import { ReceiptContractValidationError, validateCancelReceiptRequest, validateRecordReceiptRequest } from "../dist/index.js";
test("validates strict Receipt record and cancellation requests", () => { assert.equal(validateRecordReceiptRequest({ membershipToken: "opaque", locationId: "location-1", receiptNumber: "r-1", sourceId: "source-1", amountMinor: 1250, currency: "rsd", receiptTimestamp: "2026-08-08T10:00:00.000Z", idempotencyKey: "key-1" }).currency, "RSD"); assert.equal(validateCancelReceiptRequest({ reason: "refund", locationId: "location-1", idempotencyKey: "key-2" }).reason, "refund"); });
test("rejects floating-point money, invalid currency, unknown fields, and non-UTC timestamps", () => { assert.throws(() => validateRecordReceiptRequest({ membershipToken: "o", locationId: "l", receiptNumber: "r", sourceId: "s", amountMinor: 1.5, currency: "EUR", receiptTimestamp: "2026-08-08T10:00:00.000Z", idempotencyKey: "k" }), ReceiptContractValidationError); assert.throws(() => validateRecordReceiptRequest({ membershipToken: "o", locationId: "l", receiptNumber: "r", sourceId: "s", amountMinor: 1, currency: "€", receiptTimestamp: "2026-08-08T10:00:00.000Z", idempotencyKey: "k" }), ReceiptContractValidationError); assert.throws(() => validateCancelReceiptRequest({ reason: "r", locationId: "l", idempotencyKey: "k", receiptId: "internal" }), ReceiptContractValidationError); assert.throws(() => validateRecordReceiptRequest({ membershipToken: "o", locationId: "l", receiptNumber: "r", sourceId: "s", amountMinor: 1, currency: "EUR", receiptTimestamp: "2026-08-08", idempotencyKey: "k" }), ReceiptContractValidationError); });
