import assert from "node:assert/strict";
import test from "node:test";
import { AnalyticsContractValidationError, validateAnalyticsOverviewResponse, validateAnalyticsPeriodQuery } from "../dist/index.js";

const response = { from: "2026-08-01T00:00:00.000Z", to: "2026-08-08T00:00:00.000Z", membership: { totalMembers: 1, activeMembers: 1, newMembers: 1, suspendedMembers: 0, closedMembers: 0 }, activity: { receiptCount: 1, qualifyingReceiptCount: 1, qualifyingPurchaseAmount: [{ currency: "EUR", amountMinor: 1250 }] }, rewardPoints: { pointsEarned: 100, pointsRedeemed: 40, pointsExpired: 0 }, redemption: { redemptionCount: 1, redemptionPoints: 40, uniqueRedeemingMembers: 1 }, engagement: { activeMembersWithActivity: 1, earningMembers: 1, redeemingMembers: 1 }, statusDistribution: [{ statusLevelId: "silver", memberCount: 1 }] };
test("validates explicit-period grouped analytics responses", () => { assert.deepEqual(validateAnalyticsPeriodQuery({ from: response.from, to: response.to }), { from: response.from, to: response.to }); assert.equal(validateAnalyticsOverviewResponse(response).activity.qualifyingPurchaseAmount[0].currency, "EUR"); });
test("rejects PII, mixed duplicate currency rows, and invalid windows", () => { assert.throws(() => validateAnalyticsPeriodQuery({ from: response.to, to: response.from }), AnalyticsContractValidationError); assert.throws(() => validateAnalyticsOverviewResponse({ ...response, phone: "+381641234567" }), AnalyticsContractValidationError); assert.throws(() => validateAnalyticsOverviewResponse({ ...response, activity: { ...response.activity, qualifyingPurchaseAmount: [{ currency: "EUR", amountMinor: 1 }, { currency: "EUR", amountMinor: 2 }] } }), AnalyticsContractValidationError); });
