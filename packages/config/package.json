{
  "name": "@loyalty-platform/config",
  "version": "0.0.0",
  "private": true,
  "description": "Configuration package placeholder.",
  "scripts": {
    "build": "node -e \"console.log('config placeholder build')\"",
    "lint": "node -e \"console.log('config placeholder lint')\"",
    "test": "node -e \"console.log('config placeholder test')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  }
}
